{% extends 'base.html.twig' %}

{% block title %}NationalProgram{% endblock %}

{% block body %}
    <h1>NationalProgram</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ national_program.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ national_program.name }}</td>
            </tr>
            <tr>
                <th>Priority</th>
                <td>{{ national_program.priority }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('national_program_index') }}">back to list</a>

    <a href="{{ path('national_program_edit', {'id': national_program.id}) }}">edit</a>

    {{ include('national_program/_delete_form.html.twig') }}
{% endblock %}
