{% extends 'base.html.twig' %}

{% block title %}ReportLength{% endblock %}

{% block body %}
    <h1>ReportLength</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ report_length.id }}</td>
            </tr>
            <tr>
                <th>DateTime</th>
                <td>{{ report_length.dateTime }}</td>
            </tr>
            <tr>
                <th>Length</th>
                <td>{{ report_length.length }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('report_report_length_index') }}">back to list</a>

    <a href="{{ path('report_report_length_edit', {'id': report_length.id}) }}">edit</a>

    {{ include('report_report_length/_delete_form.html.twig') }}
{% endblock %}
